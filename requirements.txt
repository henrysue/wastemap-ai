Django>=4.2,<5.0
channels>=4.0
channels-redis>=4.0
daphne>=4.0
Pillow>=10.0
# opencv-python-headless and numpy are for real CV inference.
# The current mock classifier in consumers.py can be replaced with a real model.
opencv-python-headless>=4.8
numpy>=1.24
redis>=5.0
